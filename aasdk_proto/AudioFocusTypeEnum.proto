syntax="proto2";

package aasdk.proto.enums;

message AudioFocusType
{
  enum Enum
  {
    NONE = 0;
    GAIN = 1;
    GAIN_TRANSIENT = 2;
    GAIN_NAVI = 3;
    GAIN_TRANSIENT_GUIDANCE_ONLY = 4;
    RELEASE = 5; //was 5
  }
}

option optimize_for = SPEED;
